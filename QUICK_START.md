# 🚀 Быстрый старт

## Проблема: команда `docker-compose` не найдена

Это означает, что Docker Desktop не установлен или не запущен.

## ✅ Решение

### Вариант 1: Установить Docker Desktop (рекомендуется)

1. **Скачайте Docker Desktop:**
   - Перейдите: https://www.docker.com/products/docker-desktop
   - Нажмите "Download for Windows"
   - Сохраните файл `Docker Desktop Installer.exe`

2. **Установите Docker Desktop:**
   - Запустите установщик
   - Следуйте инструкциям
   - Перезагрузите компьютер (если требуется)

3. **Запустите Docker Desktop:**
   - Найдите Docker Desktop в меню Пуск
   - Запустите и дождитесь загрузки

4. **Проверьте установку:**
   ```powershell
   docker --version
   docker compose version
   ```

5. **Запустите проект:**
   ```powershell
   docker compose up -d
   ```

📖 **Подробные инструкции:** см. файл [INSTALL_DOCKER.md](INSTALL_DOCKER.md)

---

### Вариант 2: Использовать n8n Cloud (без Docker)

Если вы не хотите устанавливать Docker, можете использовать n8n Cloud:

1. Зарегистрируйтесь на https://n8n.cloud
2. Импортируйте workflow из `workflows/workflow.json`
3. Настройте credentials (см. README.md)

---

### Вариант 3: Установить только Docker Compose (не рекомендуется)

Если Docker Desktop установлен, но команда не работает:

1. Проверьте, что Docker Desktop запущен
2. Попробуйте использовать команду `docker compose` (без дефиса) вместо `docker-compose`
3. Перезапустите PowerShell

---

## 📝 Следующие шаги после установки Docker

1. Скопируйте `.env.example` в `.env`:
   ```powershell
   Copy-Item .env.example .env
   ```

2. Откройте `.env` и заполните:
   - `GITLAB_PRIVATE_TOKEN` - ваш GitLab токен
   - `GOOGLE_GEMINI_API_KEY` - ваш Google Gemini API ключ

3. Запустите n8n:
   ```powershell
   docker compose up -d
   ```

4. Откройте браузер: http://localhost:5678

5. Следуйте инструкциям в [README.md](README.md)


