# AI-Powered GitLab Code Review Assistant

Автоматический AI-ассистент для проведения код-ревью Merge Request в GitLab на основе Google Gemini AI.

> **Проблемы с запуском?** Если команда `docker-compose` не найдена, см. [QUICK_START.md](QUICK_START.md) для быстрого решения.

## 📋 Описание

Этот проект представляет собой n8n workflow, который автоматически анализирует код в Merge Request'ах GitLab и предоставляет детальный AI-отзыв с рекомендациями. Ассистент помогает сеньор-разработчикам проводить более качественные и быстрые ревью, фокусируясь на:

- **Качество кода**: проверка соответствия стандартам кодирования, принципам SOLID, DRY, KISS, YAGNI
- **Безопасность**: выявление SQL-инъекций, XSS-уязвимостей и других проблем безопасности
- **Производительность**: анализ потенциальных узких мест
- **Тестирование**: проверка покрытия тестами
- **Архитектура**: соответствие архитектурным решениям команды

## 🏗️ Архитектура

Workflow состоит из следующих ключевых компонентов:

### Поток выполнения:

```
1. GitLab Webhook Trigger
   ↓
2. Workflow Configuration (настройки стандартов)
   ↓
3. Получение данных MR и diff'а изменений
   ↓
4. Парсинг и формирование контекста для AI
   ↓
5. AI Code Review Agent (Google Gemini)
   ├── Simple Memory (сессия памяти)
   ├── Review Output Parser (структурированный вывод)
   └── GitLab API Tool (дополнительный доступ к GitLab)
   ↓
6. Парсинг AI-отчета
   ↓
7. Публикация результатов в GitLab MR:
   ├── Общий summary-комментарий
   ├── Индивидуальные комментарии по проблемам (до 10)
   └── Обновление лейблов MR
```

### Ключевые узлы workflow:

1. **Merge Request Event** (`gitlabTrigger`)

   - Триггер, который срабатывает при создании/обновлении Merge Request
   - Настраивается на конкретный репозиторий и события `merge_requests`

2. **Workflow Configuration** (`set`)

   - Хранит настройки стандартов кодирования
   - Критерии ревью
   - Минимальное покрытие тестами (по умолчанию 80%)

3. **Merge Request Event1** (`httpRequest`)

   - Получает полную информацию о MR через GitLab API

4. **Get MR Diff** (`httpRequest`)

   - Получает diff изменений в MR

5. **parser** (`code`)

   - Формирует структурированный контекст для AI
   - Включает title, description, diff, стандарты кодирования

6. **Code Review AI Agent** (`@n8n/n8n-nodes-langchain.agent`)

   - Основной AI-агент на базе Google Gemini
   - Анализирует код и генерирует структурированный отзыв

7. **Review Output Parser** (`outputParserStructured`)

   - Парсит ответ AI в структурированный JSON формат:
     - `summary`: краткое резюме ревью
     - `recommendation`: общая рекомендация (merge/needs_fixes/reject)
     - `issues`: массив проблем с severity, category, file, line, description, suggestion
     - `positives`: положительные аспекты кода
     - `labels`: автоматические лейблы для MR

8. **Parse AI output** (`code`)

   - Извлекает и форматирует данные из AI-ответа
   - Подготавливает данные для публикации в GitLab

9. **Post summary note** (`httpRequest`)

   - Публикует общий summary-комментарий в MR

10. **Split issues to comments** (`code`)

    - Разделяет проблемы на отдельные комментарии (максимум 10)

11. **Post issue notes** (`httpRequest`)

    - Публикует комментарии по конкретным проблемам в MR

12. **Update MR labels** (`httpRequest`)
    - Обновляет лейблы MR на основе AI-анализа
    - Возможные лейблы: `ready-for-merge`, `needs-review`, `changes-requested`, `security-review`, `performance-review`

## 🚀 Быстрый старт

### Установка Docker Desktop

Если Docker Desktop не установлен на вашем компьютере:

**Windows:**

1. Скачайте Docker Desktop для Windows: [https://www.docker.com/products/docker-desktop](https://www.docker.com/products/docker-desktop)
2. Запустите установщик `Docker Desktop Installer.exe`
3. Следуйте инструкциям мастера установки
4. После установки перезагрузите компьютер (если требуется)
5. Запустите Docker Desktop из меню Пуск
6. Дождитесь полной загрузки (иконка Docker в системном трее перестанет мигать)

**Проверка установки:**

Откройте PowerShell или командную строку и выполните:

```bash
docker --version
docker compose version
```

Если команды выполняются без ошибок, Docker установлен правильно.

> **Важно:** Убедитесь, что Docker Desktop запущен перед выполнением команд `docker compose`.

### Предварительные требования

- **Docker Desktop** (если не установлен, см. [Инструкции по установке Docker](#-установка-docker-desktop))
- GitLab Personal Access Token с правами:
  - `api`
  - `read_repository`
  - `write_repository`
- Google Gemini API ключ ([получить здесь](https://aistudio.google.com/app/apikey))

### Установка и запуск

1. **Клонируйте репозиторий:**

```bash
git clone <your-repo-url>
cd gitlab-ai-code-review
```

2. **Настройте переменные окружения:**

```bash
cp .env.example .env
```

Откройте `.env` и заполните значения:

```env
GITLAB_PRIVATE_TOKEN=glpat-your-token-here
GOOGLE_GEMINI_API_KEY=your-api-key-here
```

3. **Запустите n8n через Docker Compose:**

```bash
# Для новых версий Docker Desktop (рекомендуется)
docker compose up -d

# Или для старых версий
docker-compose up -d
```

> **Примечание:** Если команда `docker` не распознается, убедитесь, что Docker Desktop установлен и запущен. См. раздел [Установка Docker Desktop](#-установка-docker-desktop).

4. **Откройте n8n UI:**

Откройте браузер и перейдите на `http://localhost:5678`

5. **Импортируйте workflow:**

   - В n8n UI нажмите на меню (три линии в левом верхнем углу)
   - Выберите `Workflows` → `Import from File`
   - Загрузите файл `workflows/workflow.json`

6. **Настройте credentials в n8n:**

   **GitLab OAuth2 Credentials:**

   - Откройте узел `Merge Request Event`
   - Нажмите на credential и создайте новую GitLab OAuth2 credential
   - Введите ваш GitLab Personal Access Token
   - Повторите для узлов `GitLab API Tool`, `Post summary note`, `Post issue notes`, `Update MR labels`

   **Google Gemini API Credentials:**

   - Откройте узлы `Google Gemini Chat Model` и `Google Gemini Chat Model1`
   - Создайте новую Google Gemini (PaLM) API credential
   - Введите ваш `GOOGLE_GEMINI_API_KEY` из `.env`

7. **Настройте репозиторий GitLab:**

   - Откройте узел `Merge Request Event`
   - Измените `owner` и `repository` на ваши значения
   - Повторите для узла `GitLab API Tool`

8. **Активируйте workflow:**

   - Переключите переключатель `Active` в верхней части workflow
   - Скопируйте Webhook URL из узла `Merge Request Event`

9. **Настройте GitLab Webhook:**

   - Перейдите в ваш GitLab проект: `Settings` → `Webhooks`
   - Добавьте новый webhook:
     - URL: скопированный Webhook URL из n8n
     - Trigger: выберите `Merge request events`
     - Убедитесь, что включен SSL verification (если используете HTTPS)

## 📖 Детальные инструкции по развертыванию

### 1. Получение GitLab Personal Access Token

1. Перейдите в GitLab: `Settings` → `Access Tokens` (или [прямая ссылка](https://gitlab.com/-/user_settings/personal_access_tokens))
2. Создайте новый токен:
   - Name: `n8n-code-review`
   - Scopes: выберите `api`, `read_repository`, `write_repository`
   - Expiration: установите по необходимости
3. Скопируйте токен (он показывается только один раз!)
4. Добавьте токен в `.env` файл

### 2. Получение Google Gemini API Key

1. Перейдите на [Google AI Studio](https://aistudio.google.com/app/apikey)
2. Войдите в аккаунт Google
3. Нажмите `Create API Key`
4. Скопируйте ключ
5. Добавьте ключ в `.env` файл

### 3. Импорт workflow в n8n

После запуска n8n через Docker Compose:

1. Откройте `http://localhost:5678`
2. При первом запуске создайте учетную запись администратора
3. В главном меню выберите `Workflows` → `Import from File`
4. Выберите `workflows/workflow.json`
5. Workflow будет импортирован в неактивном состоянии

### 4. Настройка credentials

#### GitLab OAuth2 Credential:

1. Откройте workflow
2. Кликните на любой узел, использующий GitLab (например, `Merge Request Event`)
3. В поле `Credential for GitLab OAuth2 API` нажмите `Create New Credential`
4. Выберите `GitLab OAuth2 API`
5. В поле `Access Token` вставьте ваш `GITLAB_PRIVATE_TOKEN` из `.env`
6. Сохраните credential
7. Повторите для всех узлов, использующих GitLab API

#### Google Gemini API Credential:

1. Откройте узел `Google Gemini Chat Model`
2. В поле `Credential for Google PaLM API` нажмите `Create New Credential`
3. Выберите `Google Gemini (PaLM) API`
4. В поле `API Key` вставьте ваш `GOOGLE_GEMINI_API_KEY` из `.env`
5. Сохраните credential
6. Убедитесь, что тот же credential используется в узле `Google Gemini Chat Model1`

### 5. Настройка репозитория

В узле `Merge Request Event`:

- Измените `owner` на ваш GitLab username или group name
- Измените `repository` на название вашего репозитория

Повторите для узла `GitLab API Tool`.

### 6. Активация workflow

1. Переключите переключатель `Active` в верхней части workflow
2. Откройте узел `Merge Request Event`
3. Скопируйте `Webhook URL` (выглядит как `http://localhost:5678/webhook/...`)

**Важно:** Если вы запускаете n8n локально, GitLab не сможет достучаться до webhook URL `localhost:5678`. Вам нужно:

- **Вариант 1:** Использовать туннель (ngrok, cloudflared, localtunnel)
- **Вариант 2:** Развернуть n8n на публичном сервере
- **Вариант 3:** Использовать n8n.cloud

### 7. Настройка GitLab Webhook

1. Перейдите в ваш GitLab проект
2. `Settings` → `Webhooks`
3. Нажмите `Add webhook`
4. Заполните:
   - **URL**: вставьте Webhook URL из n8n
   - **Secret token**: опционально, для дополнительной безопасности
   - **Trigger**: выберите `Merge request events`
   - **Enable SSL verification**: включите, если используете HTTPS
5. Сохраните webhook

### 8. Тестирование

1. Создайте тестовый Merge Request в вашем GitLab репозитории
2. Проверьте выполнение workflow в n8n:
   - Перейдите в `Executions` в левом меню
   - Должно появиться новое выполнение
3. Проверьте комментарии в вашем MR - должен появиться AI-отзыв

## 🔧 Docker Compose

Проект включает `docker-compose.yml` для быстрого развертывания n8n:

```yaml
version: "3.8"

services:
  n8n:
    image: n8nio/n8n
    container_name: gitlab-ai-code-review
    ports:
      - "5678:5678"
    environment:
      - N8N_BASIC_AUTH_ACTIVE=true
      - N8N_BASIC_AUTH_USER=admin
      - N8N_BASIC_AUTH_PASSWORD=admin
      - N8N_HOST=localhost
      - N8N_PORT=5678
      - N8N_PROTOCOL=http
      - GENERIC_TIMEZONE=UTC
    volumes:
      - n8n_data:/home/node/.n8n
      - ./workflows:/data/workflows
    env_file:
      - .env

volumes:
  n8n_data:
```

### Переменные окружения Docker Compose

Вы можете настроить следующие переменные в `docker-compose.yml`:

- `N8N_BASIC_AUTH_USER` - имя пользователя для базовой аутентификации
- `N8N_BASIC_AUTH_PASSWORD` - пароль для базовой аутентификации
- `N8N_HOST` - хост для n8n (измените на ваш домен при деплое)
- `N8N_PORT` - порт n8n
- `GENERIC_TIMEZONE` - временная зона

## 📝 Пример использования

### Сценарий: Создание MR и автоматический отзыв

1. **Разработчик создает Merge Request:**

```bash
git checkout -b feature/new-feature
# ... вносит изменения ...
git commit -m "Add new feature"
git push origin feature/new-feature
# Создает MR через GitLab UI
```

2. **GitLab отправляет webhook в n8n**

3. **Workflow автоматически:**

   - Получает данные MR и diff изменений
   - Анализирует код с помощью Google Gemini AI
   - Проверяет:
     - Соответствие стандартам кодирования
     - Безопасность (SQL-инъекции, XSS)
     - Производительность
     - Покрытие тестами
     - Качество кода

4. **AI генерирует структурированный отзыв:**

```json
{
  "summary": "Обнаружено несколько проблем, требующих внимания...",
  "recommendation": "needs_fixes",
  "issues": [
    {
      "severity": "critical",
      "category": "security",
      "file": "src/auth.js",
      "line": 42,
      "description": "Потенциальная SQL-инъекция...",
      "suggestion": "Используйте prepared statements..."
    }
  ],
  "positives": ["Хорошая структура кода", "Понятные имена переменных"],
  "labels": ["changes-requested", "security-review"]
}
```

5. **Workflow публикует результаты в MR:**

   - Общий комментарий с summary и рекомендацией
   - Индивидуальные комментарии по каждой проблеме (до 10)
   - Автоматические лейблы

6. **Разработчик видит отзыв в GitLab и исправляет проблемы**

## 🔐 Переменные окружения

Полный список переменных окружения, которые нужно настроить:

| Переменная              | Описание                     | Где получить                                                                 |
| ----------------------- | ---------------------------- | ---------------------------------------------------------------------------- |
| `GITLAB_PRIVATE_TOKEN`  | GitLab Personal Access Token | [GitLab Settings](https://gitlab.com/-/user_settings/personal_access_tokens) |
| `GOOGLE_GEMINI_API_KEY` | Google Gemini API ключ       | [Google AI Studio](https://aistudio.google.com/app/apikey)                   |

### Опциональные переменные:

- `GITLAB_URL` - URL вашего GitLab инстанса (по умолчанию `https://gitlab.com`)
- `GITLAB_OWNER` - владелец репозитория (можно настроить в workflow)
- `GITLAB_REPOSITORY` - название репозитория (можно настроить в workflow)

## 🛠️ Настройка стандартов кодирования

Вы можете настроить стандарты кодирования, которые AI будет проверять, изменив узел `Workflow Configuration`:

1. Откройте workflow в n8n
2. Найдите узел `Workflow Configuration`
3. Измените значения:
   - `codingStandards` - стандарты кодирования
   - `reviewCriteria` - критерии ревью
   - `minTestCoverage` - минимальное покрытие тестами (%)

Также можно изменить стандарты в коде узла `parser`, где они определены как константы.

## 📊 Структура проекта

```
gitlab-ai-code-review/
├── workflows/
│   └── workflow.json          # Основной n8n workflow (очищенный от токенов)
├── .env.example               # Пример файла с переменными окружения
├── .gitignore                 # Игнорируемые файлы (включая .env)
├── docker-compose.yml         # Docker Compose конфигурация для n8n
└── README.md                  # Документация проекта
```
